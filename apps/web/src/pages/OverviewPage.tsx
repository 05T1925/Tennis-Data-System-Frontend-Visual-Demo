import { PageIntro } from '../components/PageIntro';

export function OverviewPage() {
  return (
    <PageIntro
      eyebrow="工程骨架"
      title="系统概览"
      description="阶段 1 仅建立前端工程与导航，任务数据和业务看板将在后续阶段实现。"
    />
  );
}
